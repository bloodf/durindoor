## Scope

Ports upstream decolua/9router direct commit `831001c3225ed6330b901720c6c2cbb1bdf41ec3` (`feat(providers): add max height and scroll for connection list`) into the fork.

Two one-line class changes, applied to the fork's Durin DS versions of both files:

- `src/app/(dashboard)/dashboard/providers/[id]/page.js` — `connectionsList` container gains `max-h-[500px] overflow-y-auto pr-1`
- `src/app/(dashboard)/dashboard/providers/components/ConnectionsCard.js` — connection list container gains `max-h-[500px] overflow-y-auto pr-1`

## Path taken: real port (behavior was missing)

The fork's v3.20.0/4.0.0 Durin DS dashboard rewrite kept both connection-list containers (`<div className="... divide-y divide-black/[0.03] dark:divide-white/[0.03]">`) without any max-height or scroll behavior, so the upstream change is genuinely missing and was ported. This is **not** a verification-only commit.

Durin DS conformance: the added classes are pure layout utilities (`max-h-[500px]`, `overflow-y-auto`, `pr-1`) — no hex values and no raw Tailwind palette utilities were introduced. The pre-existing `divide-black/[0.03]` classes are untouched (carried over from upstream by the rewrite, out of scope for this sync).

Cherry-pick conflicts in both files were formatting-only (the Durin DS rewrite restructured JSX indentation/ternary style); resolved by keeping the fork's HEAD structure and appending the three utility classes to the two target containers. Net diff is identical to upstream: 2 files, +2/-2.

## Test coverage

- `cd tests && npm run test:ci` → exit 0. Raw failures: 0, known failures still failing: 0, stale baseline entries: 0.
- `npm run lint` → exit 0 (0 errors, 256 pre-existing warnings; anti-slop/oxlint clean-tree gate OK across 1701 files).
- No new unit test added: this is a styling-only class change on existing rendered containers; per AGENTS.md §1 tests are required for runtime-behavior changes, and asserting a className string would be a brittle non-behavioral test. Existing suite covers the page structure.

## Doc coverage

Styling-only port with no behavior/API surface change; the PR body plus the commit message trailer serve as the documentation per AGENTS.md §1. The commit body records the upstream source hash, the path-taken rationale, and the conflict-resolution note.

## Baseline impact

None. `tests/__baseline__/known-fails.txt` untouched; zero raw failures; zero stale entries.

## Wire-format / migration concerns

None. Dashboard presentation change only; no API keys, headers, data-directory, or CLI-config surface is affected.
