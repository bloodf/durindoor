---
title: Postgres migrations
description: SQLite-to-PG generator, check:postgres-migrations, the DDL translator, and the adapter contract.
---

SQLite is the source of truth for schema. The opt-in Postgres engine keeps a parallel migration set under `src/lib/db/migrations/postgres/`. Operator cutover, rollback, and compose profiles are on [Postgres](../operations/postgres.mdx). Add a SQLite migration here, then regenerate the PG files.

The PG engine is DurinDoor-owned. Do not send a PR to `decolua/9router` for it.

## Layout

```text
src/lib/db/
  schema.js                         TABLES, the declarative SQLite schema
  migrations/                       SQLite set (001-018 plus helpers)
  migrations/postgres/              generated PG set (do not hand-edit)
  dialects/postgres/translate.js    SQLite DDL -> PG DDL
  adapters/pgAdapter.js             same run/get/all/exec/transaction surface
  cutover.js                        test, migrate, mirror, flip
  postgresFallback.js               boot fallback to SQLite
  postgresCapabilityGate.js         per-version feature map
  secrets.js                        encrypted postgresUrl on the settings row
```

SQLite latest version is 18 (`018-api-key-groups.js`, registered in `migrations/index.js`). PG latest version is also 18, but that file is `018-pg-cutover-log.js`, a PG-only table. Additive tables that already exist in `schema.js` `TABLES` (including `apiKeyGroups`) are created by PG `001-initial.js` on a fresh cluster.

## Add a migration

<Steps>
<Step>
Add `00N-name.js` under `src/lib/db/migrations/` and export `{ version, name, up(db) }`. Append it to `migrations/index.js`. `up` receives the SQLite adapter.

</Step>
<Step>
If the change is a new column on an existing table, add the column to the `TABLES` entry in `schema.js` as well. PG `001-initial.js` is generated from `TABLES`, so a column that exists only in a later SQLite file never appears on a fresh PG install.

</Step>
<Step>
Regenerate the PG set:

```bash
node scripts/migrate-sqlite-ddl-to-pg.mjs
npm run check:postgres-migrations
```

`check:postgres-migrations` is `node scripts/check-postgres-migrations.mjs`, which runs the generator with `--check` and exits 1 on drift.

</Step>
<Step>
Most additive SQLite files become version-stamp stubs on PG because bootstrap already created the table. The generator's `NO_OP_MIGRATIONS` set lists those filenames. `009-encrypt-credentials.js` is a separate no-op (rows mirrored from SQLite are already encrypted; new writes go through `columnCrypto`). `018-pg-cutover-log.js` is always emitted as the PG-only log table.

Unhandled numbered SQLite files are not emitted. Put a true no-op in `NO_OP_MIGRATIONS`, or add a special case next to `009` / `017`, then re-run the generator.

</Step>
</Steps>

Do not edit files under `migrations/postgres/` by hand. Each generated file starts with `AUTO-GENERATED by scripts/migrate-sqlite-ddl-to-pg.mjs` and `do not edit`.

## DDL translator

`src/lib/db/dialects/postgres/translate.js` runs at generate time, not at request time.

| SQLite | Postgres |
| --- | --- |
| `INTEGER PRIMARY KEY` / `AUTOINCREMENT` | `BIGSERIAL PRIMARY KEY` |
| `INTEGER DEFAULT 0/1` boolean-looking columns | stays `INTEGER` so repos can keep `=== 1` checks |
| `TEXT`, `TEXT NOT NULL`, `TEXT PRIMARY KEY` | unchanged |
| `REAL` | `DOUBLE PRECISION` |
| `datetime('now')` | `CURRENT_TIMESTAMP` |
| `PRAGMA table_info(name)` | `information_schema.columns` query |
| `INSERT ... ON CONFLICT(...) DO UPDATE` | unchanged (PG 9.5+) |
| `CREATE INDEX ... COLLATE NOCASE` | `CREATE INDEX ... LOWER(col)` |

`last_insert_rowid()` is not rewritten in SQL. `pgAdapter` appends `RETURNING id` on `INSERT` for `BIGSERIAL` tables.

The translator does not convert JSON columns to JSONB (they stay `TEXT` for `columnCrypto`). It does not encode PG 17/18 features in DDL. The capability gate turns those on at runtime.

## Capability gate

`src/lib/db/postgresCapabilityGate.js` reads `server_version_num` and computes effective state for every key in `DEFAULT_FEATURES`. Defaults target PG 18. Entries that `require: ">=19"` stay off.

To add a toggle:

1. Add `{ enabled, requires: ">=N" }` to `DEFAULT_FEATURES`.
2. Update `CapabilityMatrix` in `src/app/(dashboard)/dashboard/settings/database/components/CapabilityMatrix.jsx` if the UI needs a new column.
3. Add a case in `tests/unit/db/postgresCapabilityGate.test.js` for majors 16, 17, 18, and 19.

## Adapter contract

`src/lib/db/adapters/pgAdapter.js` matches the SQLite adapters:

- `run` / `get` / `all` rewrite `?` to `$1, $2, ...`.
- `exec` splits on `;` plus newline.
- `transaction` uses `BEGIN` / `COMMIT` / `ROLLBACK` with savepoints.
- `close` is idempotent. `flush` and `checkpoint` are no-ops.
- `capabilities.isPostgres` is `true`. `serverVersionNum` / `serverVersion` come from `SHOW server_version_num` / `SHOW server_version`.

One `pg.Client` for the process. No pool, no auto-reconnect. Tests may pass `clientFactory`. Production omits it.

## Cutover lock and secrets

`cutover.js` holds an in-process mutex. A second cutover while one is running returns HTTP 503 with `Retry-After: 5` from `POST /api/settings/database/cutover`.

`src/lib/db/secrets.js` is the only sanctioned reader of the Postgres URL. New writes go to `settings.postgresUrl` (AES-256-GCM, AAD `postgres-url`). `DURINDOOR_PG_URL` wins at boot. `DATA_DIR/durindoor-secrets.json` (mode `0600`) is a read-only legacy fallback. `GET /api/settings/database` must not return the password.
