---
title: Operations
description: Startup, upgrades, data, Postgres, security, and API-key scoping.
---

Day-two work after the gateway is installed. Start and stop the process, back up `DATA_DIR`, move versions, optionally cut over to Postgres, and lock the dashboard before it leaves loopback.

Secrets, login lockout, and API-key account scoping live here too. Install steps stay in [Installation](../getting-started/installation.mdx). Bind addresses and reverse proxies stay in [Deployment](../deployment).

<Cards>
  <Card title="Startup" href="startup" description="CLI, source, and Docker start. Health, smoke test, restart, logs." />
  <Card title="Upgrading" href="upgrading" description="Backup, image or npm bump, schema migrations, rollback." />
  <Card title="Data management" href="data-management" description="SQLite file, backups, restore, and files that skip DATA_DIR." />
  <Card title="Postgres" href="postgres" description="Opt-in engine, compose profiles, cutover, fallback, rollback." />
  <Card title="Security" href="security" description="JWT_SECRET, passwords, API_KEY_SECRET, MACHINE_ID_SALT, rate limits." />
  <Card title="API key scoping" href="api-key-scoping" description="Restrict a key to named provider accounts. Zero rows stays open." />
</Cards>
