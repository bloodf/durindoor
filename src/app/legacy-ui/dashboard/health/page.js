// Mounted copy of the pre-rewrite dashboard, served when a reader has not
// opted into the redesign. The implementation lives in the frozen snapshot.
export { default } from "@/legacy/pages/dashboard/health/page";
